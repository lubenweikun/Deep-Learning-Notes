## 学习笔记

机器学习是一整套范式：从数据里自动学出 输入 → 输出 的映射，在人工给定的特征上拟合 x 到 y 的映射：定损失、算梯度、调参数。

深度学习是它的一个分支方法——用多层神经网络自动学出分层表示，把映射写成多层复合函数

大模型又是深度学习的一支：机制没变，在海量语料上预训练出的通用序列模型把一切任务改写成预测下一个词，学的是一个序列上的条件概率分布

共同点：都是数据 + 目标函数 + 优化。所有差异都来自同一个问题的不同答案——特征由谁来设计、要多少数据和算力、能力有多通用、可控性有多强
